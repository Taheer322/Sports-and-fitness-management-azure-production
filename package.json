{
  "name": "fitness-gym-management",
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend/fitness",
    "build": "npm run build --prefix frontend/fitness",
    "start": "npm start --prefix backend"
  }
}
